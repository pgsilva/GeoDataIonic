import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ArgentinaPage } from './argentina';

@NgModule({
  declarations: [
    ArgentinaPage,
  ],
  imports: [
    IonicPageModule.forChild(ArgentinaPage),
  ],
})
export class ArgentinaPageModule {}
